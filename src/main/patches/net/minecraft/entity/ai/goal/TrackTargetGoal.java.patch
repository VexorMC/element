--- TrackTargetGoal.java
+++ TrackTargetGoal.java
@@ -9,11 +9,11 @@
 import net.minecraft.entity.attribute.EntityAttributes;
 import net.minecraft.entity.mob.MobEntity;
 import net.minecraft.entity.player.PlayerEntity;
 import net.minecraft.scoreboard.AbstractTeam;
 import net.minecraft.util.math.BlockPos;
-import net.minecraft.util.math.MathHelper;
+import net.minecraft.util.Mth;
 import org.apache.commons.lang3.StringUtils;
 
 public abstract class TrackTargetGoal extends Goal {
 	protected final PathAwareEntity mob;
 	protected boolean checkVisibility;
@@ -144,12 +144,12 @@
 		} else {
 			PathNode pathNode = path.getEnd();
 			if (pathNode == null) {
 				return false;
 			} else {
-				int i = pathNode.posX - MathHelper.floor(livingEntity.x);
-				int j = pathNode.posZ - MathHelper.floor(livingEntity.z);
+				int i = pathNode.posX - Mth.floor(livingEntity.x);
+				int j = pathNode.posZ - Mth.floor(livingEntity.z);
 				return (double)(i * i + j * j) <= 2.25;
 			}
 		}
 	}
 }
