--- BookEditScreen.java
+++ BookEditScreen.java
@@ -1,7 +1,8 @@
 package net.minecraft.client.gui.screen.ingame;
 
+import com.element.render.MatrixStack;
 import com.google.common.collect.Lists;
 import com.google.gson.JsonParseException;
 import com.mojang.blaze3d.platform.GlStateManager;
 import io.netty.buffer.Unpooled;
 import java.util.List;
@@ -282,11 +283,11 @@
 			this.setPageContent(string3);
 		}
 	}
 
 	@Override
-	public void render(int i, int j, float f) {
+	public void render(MatrixStack matrices, int j, float f, int i) {
 		GlStateManager.color(1.0F, 1.0F, 1.0F, 1.0F);
 		this.client.getTextureManager().bindTexture(TEXTURE);
 		int k = (this.width - this.pageWidth) / 2;
 		int l = 2;
 		this.drawTexture(k, l, 0, 0, this.pageWidth, this.pageHeight);
@@ -358,11 +359,11 @@
 					this.renderTextHoverEffect(text3, i, j);
 				}
 			}
 		}
 
-		super.render(i, j, f);
+		super.render(matrices, j, f, i);
 	}
 
 	@Override
 	protected void mouseClicked(int i, int j, int k) {
 		if (k == 0) {
