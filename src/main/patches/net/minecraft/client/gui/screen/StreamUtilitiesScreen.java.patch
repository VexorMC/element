--- StreamUtilitiesScreen.java
+++ StreamUtilitiesScreen.java
@@ -1,7 +1,8 @@
 package net.minecraft.client.gui.screen;
 
+import com.element.render.MatrixStack;
 import com.google.common.collect.Lists;
 import java.util.List;
 import java.util.Set;
 import net.minecraft.client.gui.widget.ButtonWidget;
 import net.minecraft.client.resource.language.I18n;
@@ -157,18 +158,18 @@
 			this.client.setScreen(null);
 		}
 	}
 
 	@Override
-	public void render(int i, int j, float f) {
+	public void render(MatrixStack matrices, int j, float f, int i) {
 		this.renderBackground();
 		this.drawCenteredString(this.textRenderer, this.chatUserName.asUnformattedString(), this.width / 2, 70, 16777215);
 		int k = 80;
 
 		for (Text text : this.displayTexts) {
 			this.drawWithShadow(this.textRenderer, text.asFormattedString(), this.textX, k, 16777215);
 			k += this.textRenderer.fontHeight;
 		}
 
-		super.render(i, j, f);
+		super.render(matrices, j, f, i);
 	}
 }
