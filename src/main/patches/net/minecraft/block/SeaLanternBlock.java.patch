--- SeaLanternBlock.java
+++ SeaLanternBlock.java
@@ -4,11 +4,11 @@
 import net.minecraft.block.material.Material;
 import net.minecraft.block.material.MaterialColor;
 import net.minecraft.item.Item;
 import net.minecraft.item.Items;
 import net.minecraft.item.itemgroup.ItemGroup;
-import net.minecraft.util.math.MathHelper;
+import net.minecraft.util.Mth;
 
 public class SeaLanternBlock extends Block {
 	public SeaLanternBlock(Material material) {
 		super(material);
 		this.setItemGroup(ItemGroup.BUILDING_BLOCKS);
@@ -19,11 +19,11 @@
 		return 2 + random.nextInt(2);
 	}
 
 	@Override
 	public int getBonusDrops(int i, Random random) {
-		return MathHelper.clamp(this.getDropCount(random) + random.nextInt(i + 1), 1, 5);
+		return Mth.clamp(this.getDropCount(random) + random.nextInt(i + 1), 1, 5);
 	}
 
 	@Override
 	public Item getDropItem(BlockState blockState, Random random, int i) {
 		return Items.PRISMARINE_CRYSTALS;
