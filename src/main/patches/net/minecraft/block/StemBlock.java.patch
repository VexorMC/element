--- StemBlock.java
+++ StemBlock.java
@@ -9,11 +9,11 @@
 import net.minecraft.state.StateManager;
 import net.minecraft.state.property.DirectionProperty;
 import net.minecraft.state.property.IntProperty;
 import net.minecraft.util.math.BlockPos;
 import net.minecraft.util.math.Direction;
-import net.minecraft.util.math.MathHelper;
+import net.minecraft.util.Mth;
 import net.minecraft.world.BlockView;
 import net.minecraft.world.World;
 
 public class StemBlock extends PlantBlock implements Growable {
 	public static final IntProperty AGE = IntProperty.of("age", 0, 7);
@@ -78,11 +78,11 @@
 			}
 		}
 	}
 
 	public void grow(World world, BlockPos blockPos, BlockState blockState) {
-		int i = (Integer)blockState.get(AGE) + MathHelper.nextInt(world.random, 2, 5);
+		int i = (Integer)blockState.get(AGE) + Mth.nextInt(world.random, 2, 5);
 		world.setBlockState(blockPos, blockState.with(AGE, Math.min(7, i)), 2);
 	}
 
 	@Override
 	public int getColor(BlockState blockState) {
