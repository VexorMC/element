--- FurnaceOutputSlot.java
+++ FurnaceOutputSlot.java
@@ -5,11 +5,11 @@
 import net.minecraft.entity.player.PlayerEntity;
 import net.minecraft.inventory.Inventory;
 import net.minecraft.item.ItemStack;
 import net.minecraft.item.Items;
 import net.minecraft.recipe.SmeltingRecipeRegistry;
-import net.minecraft.util.math.MathHelper;
+import net.minecraft.util.Mth;
 
 public class FurnaceOutputSlot extends Slot {
 	private PlayerEntity player;
 	private int amount;
 
@@ -51,12 +51,12 @@
 			int i = this.amount;
 			float f = SmeltingRecipeRegistry.getInstance().getXp(itemStack);
 			if (f == 0.0F) {
 				i = 0;
 			} else if (f < 1.0F) {
-				int j = MathHelper.floor((float)i * f);
-				if (j < MathHelper.ceil((float)i * f) && Math.random() < (double)((float)i * f - (float)j)) {
+				int j = Mth.floor((float)i * f);
+				if (j < Mth.ceil((float)i * f) && Math.random() < (double)((float)i * f - (float)j)) {
 					j++;
 				}
 
 				i = j;
 			}
