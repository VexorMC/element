--- ScreenHandler.java
+++ ScreenHandler.java
@@ -9,11 +9,11 @@
 import net.minecraft.entity.player.PlayerInventory;
 import net.minecraft.inventory.Inventory;
 import net.minecraft.inventory.slot.Slot;
 import net.minecraft.item.Item;
 import net.minecraft.item.ItemStack;
-import net.minecraft.util.math.MathHelper;
+import net.minecraft.util.Mth;
 
 public abstract class ScreenHandler {
 	public List<ItemStack> trackedStacks = Lists.newArrayList();
 	public List<Slot> slots = Lists.newArrayList();
 	public int syncId;
@@ -511,11 +511,11 @@
 	}
 
 	public static void calculateStackSize(Set<Slot> set, int i, ItemStack itemStack, int j) {
 		switch (i) {
 			case 0:
-				itemStack.count = MathHelper.floor((float)itemStack.count / (float)set.size());
+				itemStack.count = Mth.floor((float)itemStack.count / (float)set.size());
 				break;
 			case 1:
 				itemStack.count = 1;
 				break;
 			case 2:
@@ -547,9 +547,9 @@
 					i++;
 				}
 			}
 
 			f /= (float)inventory.getInvSize();
-			return MathHelper.floor(f * 14.0F) + (i > 0 ? 1 : 0);
+			return Mth.floor(f * 14.0F) + (i > 0 ? 1 : 0);
 		}
 	}
 }
